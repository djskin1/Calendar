<Window x:Class="Calendar.LocalAdminLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource LoginLoc}" 
        Height="650" 
        Width="620"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Icon="/Images/icon.png">

    <Window.Resources>

        <!-- Primary button -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SidebarBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SidebarBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="Background" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small utility button -->
        <Style x:Key="SmallButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource SidebarBrush}">

            <Grid Margin="30,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="70"
                        Height="70"
                        Background="{DynamicResource SurfaceBrush}"
                        CornerRadius="10"
                        Padding="8"
                        VerticalAlignment="Center">

                    <Image Source="/Images/Logo.png" 
                           Stretch="Uniform"/>
                </Border>

                <StackPanel Grid.Column="1"
                            Margin="15,0,0,0"
                            VerticalAlignment="Center">

                    <TextBlock Text="{DynamicResource AppName}" 
                               FontSize="23" 
                               FontWeight="SemiBold" 
                               Foreground="{DynamicResource SurfaceBrush}"/>
                    <TextBlock Text="{DynamicResource Locadmin}" 
                               FontSize="13" 
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,4,0,0"/>

                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Login -->
        <Border Grid.Row="1"
                Margin="30"
                Padding="36,30"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="14"
                BorderThickness="1">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                            Text="{DynamicResource adminsignin}"
                           FontSize="21"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                <TextBlock Grid.Row="1"
                            Text="{DynamicResource loginsub}"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="12"
                           Margin="0,5,0,22"/>

                <!-- Username -->
                <TextBlock Grid.Row="2"
                            Text="{DynamicResource Username}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                <Border Grid.Row="3"
                        Height="52"
                        Margin="0,8,0,18"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="{DynamicResource SurfaceBrush}"
                                CornerRadius="10,0,0,10"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="0,0,1,0">
                            <TextBlock Text="&#xE77B;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="18"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>

                        <Grid Grid.Column="1"
                              Margin="15,0">
                            <TextBox x:Name="UsernameTextBox"
                                     Background="{DynamicResource TransparentBrush}"
                                     Foreground="{DynamicResource PrimaryTextBrush}"
                                     BorderThickness="0"
                                     FontSize="15"
                                     VerticalAlignment="Center"
                                     TextChanged="UsernameTextBox_TextChanged"/>
                            <TextBlock x:Name="UsernamePlaceholderText"
                                       Text="{DynamicResource UserPlace}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       FontSize="14"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>

                        </Grid>
                    </Grid>
                </Border>

                <!-- Password -->
                <TextBlock Grid.Row="4"
                           Text="{DynamicResource Password}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SecondaryTextBrush}"/>

                <Border Grid.Row="5"
                        Height="52"
                        Margin="0,8,0,10"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="52"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="82"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="{DynamicResource SurfaceBrush}"
                                CornerRadius="10,0,0,10"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="0,0,1,0">
                            <TextBlock Text="&#xE72E;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="18"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <Grid Grid.Column="1"
                              Margin="15,0">
                            <PasswordBox x:Name="PasswordTextBox"
                                         Background="{DynamicResource TransparentBrush}"
                                         Foreground="{DynamicResource PrimaryTextBrush}"
                                         BorderThickness="0"
                                         FontSize="15"
                                         VerticalContentAlignment="Center"
                                         KeyDown="PasswordTextBox_KeyDown"/>
                            <TextBox x:Name="VisiblePasswordTextBox"
                                     Background="{DynamicResource TransparentBrush}"
                                     BorderThickness="0"
                                     FontSize="15"
                                     VerticalContentAlignment="Center"
                                     Visibility="Collapsed"
                                     TextChanged="VisiblePasswordTextBox_TextChanged"
                                     KeyDown="VisiblePasswordTextBox_KeyDown"/>

                            <TextBlock x:Name="PasswordPlaceholderText"
                                       Text="{DynamicResource PassPlace}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       FontSize="14"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>
                        </Grid>
                        <Border Grid.Column="2"
                                Background="{DynamicResource SurfaceBrush}"
                                CornerRadius="0,10,10,0"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1,0,0,0">
                            <Button x:Name="ShowPasswordButton"
                                    Content="{DynamicResource Show}"
                                    Width="58"
                                    Height="32"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource SmallButtonStyle}"
                                    Click="ShowPasswordButton_Click"/>

                        </Border>
                    </Grid>
                </Border>

                <TextBlock x:Name="ErrorText"
                           Grid.Row="6"
                          Text=""
                           Foreground="{DynamicResource ErrorTextBrush}"
                           FontSize="12"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           Margin="0,5,0,10"
                           Visibility="Collapsed"/>

                <!-- Buttons -->
                <Grid Grid.Row="7"
                     Margin="0,15,0,0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="110"/>
                    </Grid.ColumnDefinitions>


                    <Button Grid.Column="1"
                            Content="{DynamicResource Cancel}"
                            Height="40"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="CancelButton_Click"/>

                    <Button x:Name="SignInButton"
                            Grid.Column="3"
                            Content="{DynamicResource Signin}"
                            Height="40"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Foreground="{DynamicResource PrimaryTextBrush}"
                            Click="SignInButton_Click"/>

                </Grid>
            </Grid>

        </Border>
        

    </Grid>
</Window>
